name: Reusable Workflow

on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string

jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "username: ${{ inputs.username }}"
          echo "IMAGE_TAG: ${IMAGE_TAG}"
    env:
      IMAGE_TAG: latest.00${{ github.run_number }}
